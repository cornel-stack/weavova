# Phase 1 — Data Model: T0.1 Walking Skeleton

**Not applicable to this slice.**

T0.1 introduces no data entities, no database, no schema, and no fixtures. The landing at "/" is
static and reads nothing.

Per the constitution (Principle VI, Fixtures-First) and the build plan, the schema is written
before the screens that read it — that work begins at **tier T0.3** (database / Drizzle / schema),
where the fixture shape becomes the schema contract. This file is intentionally a placeholder so
the artifact set is complete and to make explicit that no model is defined here.
